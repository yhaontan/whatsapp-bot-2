client.on('message_create', async msg => {
  // ...כל הבדיקות הקיימות שלך...

  // שליחת תגובת "ממתין"
  const waitingReply = await msg.reply('🕒 בהפצה...');

  let sentSuccess = false;
  try {
    // שליחת ההודעה ליעד...
    if (msg.hasMedia) {
      // ...שליחת מדיה
    } else {
      // ...שליחת טקסט
    }
    sentSuccess = true;
  } catch (err) {
    sentSuccess = false;
  }

  // שליחת תגובה מתאימה
  if (sentSuccess) {
    await msg.reply('✅ הופץ בהצלחה!');
  } else {
    await msg.reply('❌ הפצה נכשלה, יש בעיה!');
  }
});
